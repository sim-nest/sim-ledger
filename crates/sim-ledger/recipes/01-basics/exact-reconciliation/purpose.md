# Exact reconciliation certificate

Describes the safe reconciliation sequence: freeze statement and ledger inputs,
use bounded exact candidates only as review order, record Mia's immutable set
decisions, and independently recompute complete coverage and a zero bridge.
Correction vouchers remain balanced drafts until Mia approves them into the
book.

