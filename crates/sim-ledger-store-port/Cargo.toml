[package]
# orphan-crate-exempt: narrow provider-neutral port consumed by the ledger product and platform adapters
name = "sim-ledger-store-port"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Provider-neutral relational year-file port for SIM ledger"
readme = "README.md"

[dependencies]
sim-relation-site = { version = "0.1.0" }
sim-storage-port = { version = "0.1.0" }

[lints]
workspace = true
