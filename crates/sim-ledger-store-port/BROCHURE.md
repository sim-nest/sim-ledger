# Relational ledger placement without leakage

`sim-ledger-store-port` lets a platform materialize per-year relational files
behind opaque mounts while ledger code retains schema, plans, and accounting
policy. Applications receive narrow ledger methods rather than driver access.
